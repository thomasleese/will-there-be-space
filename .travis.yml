addons:
  postgresql: '9.3'
  apt:
    sources:
      - deadsnakes
    packages:
      - python3.4
      - python3.4-dev
      - python3.4-venv

services:
  - redis-server
  - postgresql

script:
  - bash -n script/*
  - bash script/cibuild
